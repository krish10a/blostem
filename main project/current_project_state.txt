# Blostem MVP - Current Project State
Updated: Post Module 4 Completion (Persona Mapping)

## Active Environment
- **Backend**: FastAPI (127.0.0.1:8000)
- **Frontend**: Next.js (localhost:3000)
- **Database**: local SQLite (`blostem.db`) inside `blostem-backend`
- **Vector Store**: Pinecone (`blostem-mvp-index`)

## Current Database Schema
`Prospect` Table Columns:
- `id` (Integer)
- `company_name` (String)
- `website` (String)
- `industry` (String)
- `size` (String)
- `signals` (Text) - Now stores strict JSON generated by Gemini 2.5 (signal_summary, reason_tags, raw_notes)
- `fit_score` (Float) - Computed via rule-based backend logic
- `intent_score` (Float) - Computed via rule-based backend logic
- `priority_score` (Float) - Sum of fit and intent with weights
- `confidence_score` (Float)
- `score_explanation` (Text) - AI-generated justification for the scores
- `persona_map` (Text) - JSON containing 3-5 distinct personas mapped using the Signals.
- `messages` (Text)
- `sequence_plan` (Text)
- `compliance_status` (String)
- `next_action` (String)
- `created_at` (DateTime)
- `updated_at` (DateTime)

## API Endpoints Available
- `POST /prospects/`: Creates a single manual prospect.
- `POST /prospects/upload/`: Ingests an unstructured CSV file.
- `GET /prospects/`: Returns the list of saved prospects in the DB.
- `POST /prospects/{prospect_id}/generate-signals`: Triggers Gemini 2.5 API with Google Search Grounding to extract structured signals (JSON).
- `POST /prospects/{prospect_id}/score`: Computes the rule-based Lead Score (fit, intent, priority) and generates an explanation via Gemini.
- `POST /prospects/{prospect_id}/map-personas`: Triggers Gemini 2.5 API to generate matching stakeholder personas with pain points, pitch angles, and precise tones (Output inserted into `persona_map`).

## Frontend Checkpoint
- Home Dashboard (`page.tsx`) acts as the main Account Discovery pipeline grid.
- Interactive Components created in `blostem-ui/src/components`:
  - `ProspectIntakeForm.tsx` (Manual ingester).
  - `ProspectCSVUpload.tsx` (Bulk ingester).
  - `SignalIntelligencePanel.tsx` (AI Context window with 'Extract Signals', 'Regenerate Signals', and 'Calculate AI Lead Score' buttons).
  - `PersonaMappingPanel.tsx` (Displays identified enterprise personas beneath signal intelligence).

## Next Module Focus
Module 5: Hyper-Personalized Outreach Generation (Drafting automated cadences utilizing defined personas & extracted signals).
